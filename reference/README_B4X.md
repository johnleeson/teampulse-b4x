# Reference snapshot (read-only upstream)

Files here are copied from **johnleeson/teampulse-app** for porting into B4X.

- Do **not** edit `C:\Dev\teampluse\teampulse-app` when working on this B4X project.
- Prefer updating this `reference/` folder by re-copying from that repo when the web app changes.
- Gemini (`services/geminiService.ts`) is intentionally not ported.
